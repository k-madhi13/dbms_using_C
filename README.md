# dbms_using_C

## task division
1. Erica - create database and table
2. Madhi - insert and select data
3. Isha - delete database and table or data

## program flow
main menu
- create
  - Database
    - must create a folder with given name (can use mkdir command)
    - must include a file named tableNames.txt
    - must check if database name is unique, no whitespace, no extension
  - Table
    - must create a .txt and .csv file
    - .txt must contain:

```
this_is_TableName.txt
numbersOfColumns primaryKey
(%d %d)
```
